# 2026-09-13 开发验证记录

当前工作区已实现预测提示左上角、环形电流六组、后台计算和螺线管预计算复用。本节为发布前验证快照；最终现场验收未完成，发布后的线上版本核验另见实施报告。以下本轮证据与后附历史记录分开使用。

- 自动回归83/83通过；包含45项内置物理检查、121个冻结独立参考向量、23组几何边界，以及原有8项课堂数据和10项截面测试。子检查数不能与83项测试相加。
- 环形电流18组密度/半径/方向组合验证16/40/72条实际轨迹、空间对称性、有符号方向和质量。36个螺线管尺寸缓存全部核对哈希、轨迹方向、质量和场值缩放；默认热图与直接积分逐像素一致。
- 当前Mac/Chrome，本地HTTP默认螺线管5次首次揭示中位数998.7ms，原版8702.3ms；默认含热图5次1099.3–1201.9ms。20次重复揭示P95为47.7ms，全部复用现有结果。参数修改约489.6–640.3ms，旋转约38.9FPS。
- 七场景预测/揭示、四种视口、三档环形密度、截面回归、计算中切场景、教学模式、Worker失败重试及50轮切换均留有本轮证据。50轮同场景几何/纹理数量稳定；这不是完整JavaScript堆内存泄漏证明。
- 地图入口和48桌模拟示例通过；两个XLSX文件结构有效。浏览器自动上传遭接口拒绝，本轮实际文件上传未复验。
- 原生全屏被浏览器拒绝，备用铺满窗口和退出已验证；真正125%缩放、原生全屏、授课电脑与投影仍待验。外域阻断、极端组合首显、场景进入、滚轮/复位和控制台审计已补验，详见下段。

补验：两种极端组合含热图各5次首次显示990.3–1199.1ms，10次螺线管进场景147.0–202.4ms。新一轮50次切换逐轮采集，无控制台警告/错误，资源数量稳定。CSP强制禁止外域请求并用负向对照确认生效后，七场景磁感线和热图、地图48桌示例均通过。实际滚轮放大、拖动、复位及七场景点选读数通过。当前浏览器为Chrome 152，ANGLE Metal / Intel UHD Graphics 630。

完整19项验收矩阵、计时定义与证据在同级 `需求0913/实施与验收报告.md`。本次性能结果是开发机暂验结果，不能写成授课设备最终通过。

---

以下为9月6日至7日历史记录，性能、线上版本与浏览器上传结论均属于当时版本。

# 磁场网页核验记录

2026年9月6日。本记录替代9月5日说明；历史实现不作为本版验收依据。

## 本次修正

条形磁铁由低阶端面采样改为解析积分。螺线管由48层均匀圆环叠加改为变量代换后的48点Gauss积分。去除点选坐标靠近原点时吸附，修正反向排列下只显示右导线时丢失方向。环形电流默认24条代表线，场函数为384段积分。

教室地图升级为五列XLSX和三维箭头，增加导入错误行提示、原图保留、零矢量处理、工作表选择、颜色跨度下限、锁定色标、全屏色标与选点读数。

## 数值证据

独立参考121点全部通过。非零场使用矢量相对误差≤1%，理论零场使用绝对误差≤0.000001μT。独立高阶积分与生产函数分开实现，参考自身也作积分细化。

| 场景 | 最大相对误差 |
| --- | ---: |
| 地磁 | 小于0.000001% |
| 条形磁铁 | 小于0.000001% |
| 弯折导线 | 0.00000850% |
| 无限长直导线 | 小于0.000001% |
| 双导线 | 小于0.000001% |
| 环形电流 | 0.5512% |
| 螺线管 | 0.0000138% |

默认螺线管R=15cm、L=50cm、N=160、I=50A，中心17.240918mT、轴线管口9.629120mT。轴向曲线与解析式一致，另核对非轴近绕组点。均匀轴向48/96/192/384/768层收敛记录及生产Gauss积分对照随完整课堂包提供。

内置45项物理检查、23组几何默认及参数边界检查全部通过。条形磁铁端面4组法向连续性与界面掩码回归通过。所有精度结论只适用于记录的非奇异测试点，不推断到模型正则化区或真实器材。

对称双导线镜像点分量残差为零。当前固定参数未复现历史画面的数值不对称，缺少原历史截图，不能宣称已证明其成因。

## 数据与浏览器证据

数据单元8项、地图浏览器13项通过。包括真实XLSX上传、多表选择、缺失、重复、零矢量、全相同、非法值、竖直场、稀疏布局、视图、选点、标记和全屏。当前Mac无头Chromium中100桌86.5毫秒出图。

七场景默认画面检查、全屏及390×844移动视口无横向溢出。外域请求阻止后页面错误0，资源外部依赖0。场景切换约0.85至5.31秒，软件渲染计时不能代替授课电脑实测。

9月6日在线功能版本4a5a587的8个关键文件SHA256与本地一致，在线45项物理检查和48桌XLSX导入通过。

## 2026-09-07 截面投影流线修复

双导线等流同向、Y法向、截面位置0.4、旋转0、仅截面模式：旧网格横向除以8.5且奇偶行数量相同，种子点不关于原点镜像；依次筛选轨迹导致左右内圈选到不同磁通层。复现内圈横向跨度左1.83192、右1.14288（模型单位），镜像点的磁场矢量仍正确。此前三维轨迹核验未覆盖此独立绘图路径。

修复将交错网格改为居中的9点/8点行，并在垂直导线的截面上优先从两根导线相同相对距离处独立积分。没有修改场公式、局部场值或复制镜像曲线。截图参数下左右内圈跨度均为1.11479。

新增10项截面测试，连同既有8项数据测试共18项通过。覆盖同向/反向、间距1.5/3/5、截面位置-2/0.4/2、非等流保留不对称、解析磁通函数守恒、磁场矢量与中点零场、单线及旋转截面的有限性与平面约束。45项既有物理自检通过。在Chrome中按截图条件检查，并用正视截面确认左右内圈对称。

## 现场待验收

目标电脑原生PowerPoint放映、投影后排可读性、真实班级测量、教师视频独立复核与40分钟师生彩排尚需现场记录。本机自动测试不替代这些项目。
