# 改造与验收记录

验收日期：2026-09-05

## 整体原则

1. 方向优先：右手定则、N/S 极、磁体内外方向和矢量叠加不得出错。
2. 解析优先：存在可靠解析式时直接使用；复杂几何才使用经过解析基准校验的数值积分。
3. 电流守恒：稳恒电流场景必须给出完整闭合回路，不模拟未定义回流路径的“开路稳恒电流”。
4. 场线真实：不使用直线或样条把失败轨迹强行接回起点；延伸到视窗边界的真实轨迹单独标记。
5. 轨迹可量化：检查闭合间隙、接缝切向、与 B 的对齐度和异常重复绕行。
6. 单位统一：内部按 SI 制计算，界面显示 `cm`、`A`、`μT`、`mT`；几何坐标 `1 单位 = 10 cm`。
7. 课堂可读：代表性场线不作场强定量编码；热力图与点选读数才用于比较大小。

## 本轮修改意见落实

- 七个场景的代表性场线均增加：地磁 `12→16`、条形磁铁 `8→12`、直导线 `15→18`、双导线 `21→30`、环形电流 `16→20`、螺线管 `8→19`；新增弯折导线默认 `15` 条。
- 地磁场球内改为光滑、无散度的核心电流近似，球面处与外部偶极场的法向和切向均连续，不再使用直线段拼接。
- 删除马蹄形磁铁，新增弯折导线；后者明确显示远置回流线并按完整闭合回路求场。
- 双导线支持独立调节 `I₁/I₂`；等流同向时原点严格为零，不等流时零场点按解析关系移动。
- 双导线默认相机移动到 `x=0` 对称面，消除斜视透视造成的一侧导线与场线看起来更粗的视觉假象；物理模型本身未用人为缩放修正。
- 环形电流每条磁感线只放一个方向箭头，靠近导线时按距离缩小。
- 螺线管由旧端面磁荷点阵改为 48 层有限圆环 Biot-Savart 叠加；修复管口附近数值反向尖峰，并补充轴向延伸线。
- 点选坐标由一位小数提高到两位小数；双导线对称轴附近自动吸附到精确原点。

## 物理数值验收

页面启动时直接调用生产场函数运行 `45` 项检查，当前结果为 `45/45`：

| 组别 | 数量 | 主要检查 |
| --- | ---: | --- |
| 地磁场 | 6 | 轴向/赤道比、赤道表面值、核心中心值/方向、球面矢量连续、球内 `∇·B≈0` |
| 弯折导线 | 4 | 局部无限长直线近似、`B∝I`、反向、完整闭合电流路径 |
| 直导线 | 5 | `1/r`、`B∝I`、反向、线芯内 `B∝r`、表面连续 |
| 双导线 | 5 | 等流同向中点零场、反向中点叠加、不等流零场点、非零中点、单线模式不受排列选项影响 |
| 环形电流 | 6 | 圆心与轴线解析式、极性、反向、有限线径无奇异且连续 |
| 条形磁铁 | 3 | 内部 S→N、镜像对称、赤道面方向 |
| 螺线管 | 9 | 中心/近端/管口解析式、方向、`B∝I`、反向、中央近似、管口连续、轴向单调衰减 |
| 全场景参数边界 | 7 | 数值/选项极值组合均为有限值，无 `NaN` 或奇异爆炸 |

关键数值结果：

- 环形电流圆心值与 `μ₀I/(2R)` 的相对误差小于 `0.5%`。
- 默认螺线管中心 `17.235 mT`，有限长轴线公式误差小于 `0.8%`。
- 螺线管管口内外 `L/2±0.001` 均保持正确方向且连续；中心、近端、管口、管外依次减弱。
- 双导线 `I₁=I₂=30 A` 时原点合场为 `0`；`I₁=30 A, I₂=70 A` 时原点为 `53.33 μT`。

## 场线几何验收

积分器使用 RK4。展示轨迹必须满足：

- 数值闭合回到种子邻域，且积分切向同向；不追加大跨度直线。
- 接缝段不超过中位步长的 `2.8` 倍，算法接缝切向余弦不小于 `0.90`。
- 抽样线段与 B 的对齐度 P05 不小于 `0.94`。
- 轨迹弧长/包围盒对角线小于 `10`，拦截闭合前反复绕行的伪轨迹。
- 开放轨迹必须真实沿 B 延伸到视窗边界，不伪装为闭合线。

| 默认场景 | 轨迹 | 最差 B 对齐度 | 最差接缝切向 | 最大绕曲率 |
| --- | ---: | ---: | ---: | ---: |
| 地磁场 | `16/16` 闭合 | `0.999999994` | `0.994` | `2.30` |
| 条形磁铁 | `12/12` 闭合 | `0.999999857` | `0.990` | `2.39` |
| 弯折导线 | `15/15` 闭合 | `0.999999887` | `0.937` | `4.49` |
| 通电直导线 | `18/18` 闭合 | `1.000000000` | `0.984` | `2.22` |
| 双导线（等流同向） | `30/30` 闭合 | `0.999999968` | `0.938` | `4.44` |
| 环形电流 | `20/20` 闭合 | `0.999999984` | `0.972` | `4.44` |
| 通电螺线管 | `18` 闭合 + `1` 轴向延伸 | `0.999999998` | `0.994` | `2.33` |

另验收 `16` 组参数边界组合，连同七个默认场景总结果为 `23/23`：地磁最大倾角、条形磁铁最短/最长、弯折导线紧凑/最大反向、直导线反向、双导线反向/单线/最大间距/两种不等流、圆电流最小/最大反向、螺线管反向/短宽/长窄。

等流反向双导线仍保留 `24` 条闭合阿波罗尼奥斯圆和 `3` 条延伸到视窗外的中垂分界线；不等流时取消错误的对称分界线，改为直接追踪真实叠加场。

## 交互与视觉验收

- 学生预测模式隐藏场线、热力图、采样值和诊断结果；教师模式恢复。
- 热力图默认关闭；开启后使用排除磁体/导线内部的 P98 对数色标。
- “仅显示截面”对 `B∥ = B − (B·n)n` 做二维 RK4 积分，不裁剪三维曲线。
- 双导线 `I₁/I₂` 均可独立调节；矢量演示在精确原点显示两分量与合场，坐标精确到 `0.01` 场景单位。
- 环形电流默认 `20` 条磁感线、`20` 个场方向箭头，近导线箭头缩小，无成串重叠。
- 环形电流专项检查确认 `20/20` 条线均由积分轨迹平滑闭合，没有跨越画面的直线回接段。
- 弯折导线三段可见铜线、后方回流线和局部同心线族均可辨认；不显示大批穿越画面的开放虚线。
- 螺线管默认 `18` 条闭合线和 `1` 条轴向延伸线，中轴区域不再显得“没有磁场”。
- 螺线管热力图多轮实测约 `2.6–7.1 s` 生成（受视口与运行状态影响），P98 稳定为 `17.21 mT`。
- 实测 `390×844` 移动端 `scrollWidth=390`，无横向溢出；画布 `368×298`，七个场景均可通过可滚动场景面板访问。
- 宽线使用与 Three.js r185 匹配的官方 `Line2/LineMaterial`。
- 等流同向双导线正视截图的场线颜色像素镜像相似度为 `0.9847`，画面左右对称，无控制台错误或资源失败。
- 教室磁场图在 `1280×900` 与 `390×844` 视口均无横向溢出；示例 `6×8=48` 个座位、`2` 个异常候选可正确显示，编辑异常值后候选数同步减少。
- 教室磁场图 CSV 表头固定为 `row,col,bx,by,bz,note`，导入、导出、清空确认和本地持久化均通过交互检查。

## 教室磁场图能力边界

- 工具只在教师当前浏览器中保存数据，不声称具备云端多人实时汇总。
- 热力图显示 `|B|`，箭头显示水平分量；异常候选采用中位数基线与 `median + 3×1.4826×MAD` 阈值，且残差阈值不低于 `5 μT`。
- 异常候选只表示“值得复测”，不自动推断磁源位置、物体类别或原因。
- 稀疏座位采样只能形成有限分辨率的二维分布，不足以唯一重建三维磁感线。

## 保留近似

- 地磁场球外只描述主偶极场；球内多项式场用于光滑表达核心电流回程，不模拟真实地核流体动力学、局部异常或年代变化。
- 永磁体使用均匀磁化等效面磁荷，不模拟磁滞、饱和与非线性磁导率。
- 弯折导线把回流路径放在后方固定距离；真实电源、接线柱和底座中的导线细节不在模型内。
- 环形电流使用均匀电流有限线芯正则化，轴线与远场保持 Biot-Savart 数值解。
- 螺线管把连续绕组离散为 `48` 个带权圆环，不逐匝模拟线径与螺距；参数范围保持 `L ≥ 2R`。
- P98 热力图和代表性场线用于高中课堂辨形，不用于工程磁通量或公差分析。
