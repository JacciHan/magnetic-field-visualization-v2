# 改造与验收记录

验收日期：2026-08-30

## 整体原则

1. 方向优先：右手定则、N/S 极、磁体内外方向和矢量叠加不得出错。
2. 规律可信：满足 `B∝I`、直导线外 `B∝1/r`、偶极子远场 `B∝1/r³` 等高中物理关系。
3. 场线真实：不用直线或样条将失败轨迹强行接回起点；边界分界线必须显式标注。
4. 轨迹质量可量化：同时检查闭合间隙、接缝切向、与 B 的对齐度和异常重复绕行。
5. 单位统一：内部按 SI 制计算，界面显示 `cm`、`A`、`μT`、`mT`；几何坐标 `1 单位 = 10 cm`。
6. 课堂可读：代表性场线不作场强定量编码；热力图与点选读数才用于比较大小。

## 物理数值验收

页面启动时直接调用生产场函数运行 36 项检查，最终结果为 `36/36`：

| 组别 | 数量 | 主要检查 |
| --- | ---: | --- |
| 地磁场 | 4 | 轴向/赤道场强比 `2`、赤道表面值、球内匀强值与方向 |
| 直导线 | 5 | `1/r`、`B∝I`、反向、线芯内 `B∝r`、表面连续 |
| 双导线 | 3 | 同向中点零场、反向中点叠加、单导线模式方向不受排列选项影响 |
| 环形电流 | 6 | 圆心与轴线解析式、极性、反向、有限线径无奇异且连续 |
| 条形磁铁 | 3 | 内部 S→N、镜像对称、赤道面方向 |
| 马蹄形磁铁 | 2 | 气隙与 U 形磁路方向、中央小区域均匀性 |
| 螺线管 | 6 | 有限长中心式、方向、`B∝I`、反向、中央区近似、端部效应 |
| 全场景参数边界 | 7 | 数值/选项极值组合均为有限值，无 `NaN`/奇异爆炸 |

圆电流中心值与 `μ₀I/(2R)` 的相对误差小于 `0.5%`；螺线管中心值与有限长修正式的相对误差小于 `3.5%`。

## 场线几何验收

积分器使用 RK4。展示前必须通过以下门槛：

- 数值闭合必须回到种子邻域，且积分切向同向；不追加大跨度直线。
- 接缝段不超过中位步长的 `2.8` 倍，算法接缝切向余弦不小于 `0.90`。
- 抽样线段与 B 的对齐度 P05 不小于 `0.94`。
- 轨迹弧长/包围盒对角线小于 `10`，拦截闭合前反复绕行的伪轨迹。
- 均匀磁化球表面由等效面电流导致的真实切向跃变单独标记，不与算法拼接混淆。

| 默认场景 | 轨迹 | 最差 B 对齐度 | 最差算法接缝切向 | 最大绕曲率 |
| --- | ---: | ---: | ---: | ---: |
| 地磁场 | `12/12` 闭合 | `0.99999990` | 物理界面折角 | `2.38` |
| 条形磁铁 | `8/8` 闭合 | `0.99999991` | `0.990` | `2.36` |
| 马蹄形磁铁 | `5/5` 闭合 | `0.999861` | `0.980` | `2.39` |
| 通电直导线 | `15/15` 闭合 | `1.000000` | `0.984` | `2.22` |
| 双导线（同向） | `21/21` 闭合 | `0.99999998` | `0.974` | `2.26` |
| 环形电流 | `16/16` 闭合 | `0.99999998` | `0.972` | `4.44` |
| 通电螺线管 | `8/8` 闭合 | `0.999982` | `0.998` | `2.31` |

另外验收 14 组控件/边界组合，总结果为 `21/21`：地磁最大倾角、条形磁铁最短/最长、马蹄最小/最大气隙、直导线反向、双导线反向/单线/最大间距、圆电流最小/最大半径与反向、螺线管反向/最短最宽/最长最窄。

双导线反向时为 `24` 条闭合阿波罗尼奥斯圆和 `3` 条延伸到视窗外的中垂分界线；分界线使用虚线和动态图例，不计作失败闭合。

## 交互与视觉验收

- 学生预测模式隐藏场线、热力图、采样值和诊断结果；教师模式恢复。
- 热力图默认关闭；开启后使用排除磁体/导线内部的 P98 对数色标，图例明示 `P98`。
- “仅显示截面”对 `B∥ = B − (B·n)n` 做二维 RK4 积分，不再裁剪三维曲线；环形电流默认截面生成 `17` 条投影流线。
- 双导线单线模式禁用“同向/反向”和矢量合成控件，可视电流固定为 `+Y`；矢量读数带 `μT` 单位。
- 马蹄形磁铁默认正视极面；环形电流和螺线管的默认子午面不会侧视退化为直线。
- 螺线管视觉绕线按实际匝密度抽样，不再叠成不透明色块；物理模型仍使用界面所示实际匝数。
- 实测视口：`390×844`、`1024×768`、`1280×720`、`1920×1080`。`390 px` 时页面 `scrollWidth=390`，无横向溢出，画布为 `370×300`。
- 宽线使用与 Three.js r185 匹配的官方 `Line2/LineMaterial`，主场线不再是脆弱的单像素线。

## 保留近似

- 地磁场只描述主偶极场，不模拟地磁偏角的地区差异和局部异常。
- 永磁体使用均匀磁化的等效面磁荷模型，不模拟磁滞、饱和与非线性磁导率。
- 环形电流使用均匀电流有限线芯正则化，轴线与远场保持 Biot-Savart 数值解。
- 螺线管采用连续面电流的等效均匀磁化圆柱，不逐匝计算离散绕组；参数范围保持 `L ≥ 2R`。
- P98 热力图和代表性场线用于高中课堂辨形，不用于工程磁通量或公差分析。
